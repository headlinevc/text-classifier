#!/usr/bin/make -f

export DH_VERBOSE = 1
export DEB_CXXFLAGS_MAINT_APPEND = -O3 -march=nehalem -fwhole-program -flto 

override_dh_auto_configure:
	dh_auto_configure -- --disable-shared AR=gcc-ar NM=gcc-nm RANLIB=gcc-ranlib

%:
	dh $@  --parallel
